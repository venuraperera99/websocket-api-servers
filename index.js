const wsServer = require('./wsServer');
const apiServer = require('./apiServer');

// Both WebSocket and RESTful API servers are initialized via their respective files.
// This makes the code modular and clean.
